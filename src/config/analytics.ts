export const WEIGHTS = {
  EVENT_FREQUENCY: 0.5,
  MEMBER_COUNT: 0.2,
  RETENTION_RATE: 0.3,
} as const;

export const TARGETS = {
  EVENTS_PER_MONTH: 4,
  MEMBER_COUNT: 50,
  RETENTION_RATE: 0.75,
} as const;
